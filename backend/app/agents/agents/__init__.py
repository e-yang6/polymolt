"""
Agent definitions — one module per agent.

Each module must define a single top-level variable:

    agent: AgentConfig

The registry discovers all such modules and builds the agent list.
Add a new agent by creating a new file (e.g. my_agent.py) with an `agent` export.
"""
